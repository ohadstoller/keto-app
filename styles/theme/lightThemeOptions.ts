import { ThemeOptions } from '@mui/material/styles';

export const lightThemeOptions: ThemeOptions = {
  palette: {
    mode: 'light',
  },
};

export const darkThemeOptions: ThemeOptions = {
  palette: {
    mode: 'dark',
  },
};

export default {lightThemeOptions: lightThemeOptions, darkThemeOptions}

