import { ThemeOptions } from '@mui/material/styles';

export const themeOptions: ThemeOptions = {
  palette: {
    mode: 'light',
  },
};

export const darkThemeOptions: ThemeOptions = {
  palette: {
    mode: 'dark',
  },
};

export default {lightThemeOptions: themeOptions, darkThemeOptions}

